/**
 * Shared types - Re-exports from types directory for backward compatibility
 */

export type {
  ScadNode,
  Geometry,
  ParseResult,
  ParseError,
  EvaluateResult,
  EvaluationError,
  ModifierInfo,
  ColorInfo,
  HighlightInfo,
  GeometryObject,
  WsMessage,
  ExportOptions,
  ExportResult,
  RenderStage,
  RenderProgress,
  ProgressMessage,
} from '../types/geometry-types';
