registry = {url="https://registry.npmjs.org", token="$NPM_TOKEN"}

["registry.scopes.moicad"]
url = "https://registry.npmjs.org"
token = "$NPM_TOKEN"
[install]
exact = true
frozenLockfile = true

[test]
preload = ["./tests/setup.ts"]
timeout = 30000
