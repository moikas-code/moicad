// GUI Hooks Exports
// Custom React hooks for CAD editor functionality

export {
  useAnimation,
  type UseAnimationOptions,
  type UseAnimationReturn,
} from '../../hooks/useAnimation';

export { useEditor } from '../../hooks/useEditor';

export {
  useGeometry,
  type GeometryState,
} from '../../hooks/useGeometry';

export { useResizablePanel } from '../../hooks/useResizablePanel';

export { useViewportMenus } from '../../hooks/useViewportMenus';

export {
  useWebSocket,
  type UseWebSocketOptions,
} from '../../hooks/useWebSocket';
